NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional, temporary: set to "true" to skip the login requirement entirely.
# Anyone with the URL can then use the app with no sign-in. Leave unset
# (or anything other than "true") in production.
# DISABLE_AUTH=true
